## Summary

<!-- What does this PR do? One or two sentences. -->

## Changes

<!-- Bullet list of what changed. -->

## Testing

- [ ] `QT_QPA_PLATFORM=offscreen PYTHONPATH=src python -m unittest discover -s tests -v` — all tests pass
- [ ] `ruff check src tests` — no lint errors
- [ ] `mypy src` — no type errors
- [ ] Manually launched and verified visually (if UI changes)

## Related issues

<!-- Closes #123 -->
